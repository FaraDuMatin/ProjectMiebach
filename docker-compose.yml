services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: slatower
      POSTGRES_USER: slatower
      POSTGRES_PASSWORD: slatower
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
